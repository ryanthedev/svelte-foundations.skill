---
description: "SvelteKit coding with docs-first workflow. Loads Svelte and SvelteKit docs before writing code."
argument-hint: "[task — e.g. 'build a form with actions', 'migrate this component to Svelte 5']"
allowed-tools: ["Read", "Write", "Edit", "Glob", "Grep", "Bash", "Agent", "Skill"]
---

# /coding

## Invoke Skill

```
Skill(svelte-foundations:coding)
```
